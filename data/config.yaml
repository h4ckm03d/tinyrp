server:
  host: "localhost"
  listen_port: "8000"

resources:
  - name: A
    endpoint: /api/a/
    destination_url: "http://localhost:8080"
  - name: B
    endpoint: /api/b/
    destination_url: "http://localhost:8081"